import React from "react";
import DynamicDataPanel from "../components/DynamicDataPanel.jsx";

/**
 * DynamicPage — Route wrapper for dynamic object tables.
 */
export default function DynamicPage() {
  return <DynamicDataPanel />;
}
