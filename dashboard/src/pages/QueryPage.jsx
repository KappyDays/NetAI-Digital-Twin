import React from "react";
import QueryPanel from "../components/QueryPanel.jsx";

/**
 * QueryPage — Route wrapper for ad-hoc SQL query interface.
 */
export default function QueryPage() {
  return <QueryPanel />;
}
