import React from "react";
import StaticDataPanel from "../components/StaticDataPanel.jsx";

/**
 * StaticPage — Route wrapper for static object browser.
 */
export default function StaticPage() {
  return <StaticDataPanel />;
}
